# 视频脚本 - LaunchShield Swarm（中英对照版）

目标时长 / Target Duration：`60-90 秒 / 60-90 seconds`

定位 / Positioning：

中文：这是一条给评委看的产品演示视频。重点不是做品牌宣传，而是让人快速看懂三件事：

English: This is a product demo video for the judges. The goal is not brand promotion. The goal is to help them quickly understand three things:

- 中文：你们做了什么
- English: What you built
- 中文：为什么 Arc / Circle 对这件事关键
- English: Why Arc / Circle are essential to this workflow
- 中文：你们的产品真的跑起来了
- English: That the product is actually running end to end

## 录制建议 / Recording Tips

- 中文：录屏分辨率：`1920x1080` 或更高
- English: Record at `1920x1080` or higher.
- 中文：浏览器缩放：`100%`
- English: Keep browser zoom at `100%`.
- 中文：优先录真实运行过程
- English: Prioritize recording a real run.
- 中文：旁白、字幕二选一即可，时间紧就直接上字幕
- English: Voiceover or subtitles are both fine. If time is tight, subtitles alone are enough.
- 中文：每个镜头只讲一个重点
- English: Each shot should communicate one clear point.

## 中英对照脚本 / Bilingual Script

### 1. 开场问题 / Opening Problem（0:00 - 0:08）

画面 / Visual：

中文：项目标题页，或者首页 Hero 区。

English: Show the project title screen or the homepage hero section.

旁白 / Voiceover：

中文：

`AI 安全审计早就不是一次单点操作，而是一群高频的小工具调用。真正难的不是扫描本身，而是怎么给每一次调用定价、结算，并且保留证据。`

English:

`AI security audits are no longer a single action. They are a swarm of high-frequency tool calls. The hard part is not the scan itself. The hard part is pricing, settling, and preserving evidence for every single call.`

字幕 / Subtitle：

中文：

`AI 安全已经是一群微动作组成的协作网络。`

English:

`AI security is already a swarm of micro-actions.`

### 2. 产品一句话 / One-Line Product Description（0:08 - 0:18）

画面 / Visual：

中文：停留在首页标题和 CTA。

English: Stay on the homepage headline and CTA.

旁白 / Voiceover：

中文：

`LaunchShield Swarm 把一次 AI 安全审计拆成很多按次计费的微任务，比如仓库扫描、依赖检查、站点探测、LLM 深度分析和修复建议，并把这些调用的成本和结果实时展示出来。`

English:

`LaunchShield Swarm breaks one AI security audit into many pay-per-call micro-tasks, including repo scans, dependency checks, site probes, deep LLM analysis, and fix suggestions, then shows the cost and result of every call in real time.`

字幕 / Subtitle：

中文：

`一次审计 = 很多次付费安全工具调用`

English:

`One audit = many paid security tool calls`

### 3. 启动一次运行 / Start a Run（0:18 - 0:28）

画面 / Visual：

中文：点击 `Execute & Stress Test`，或者发起一次标准扫描。

English: Click `Execute & Stress Test`, or start a standard scan.

旁白 / Voiceover：

中文：

`我们点击一次执行，系统就会启动一整条安全审计流水线。每一步都是独立的工具调用，每一次调用都可以被单独计费、单独记录。`

English:

`With one click, the system starts a full security audit pipeline. Every step is an independent tool call, and every call can be billed and recorded on its own.`

字幕 / Subtitle：

中文：

`启动一次运行 -> 触发很多原子调用`

English:

`Start one run -> trigger many atomic invocations`

### 4. 展示 Billing Waterfall / Show the Billing Waterfall（0:28 - 0:48）

画面 / Visual：

中文：聚焦 `Streaming Billing` 或账单流水区域，让多行记录连续滚动。

English: Focus on `Streaming Billing` or the billing waterfall area, and let multiple rows keep scrolling.

旁白 / Voiceover：

中文：

`这里是整个产品最核心的部分。你看到的不只是进度条，而是一条实时账单流。每一行都代表一次真实的安全工具调用，包含工具类型、目标对象、单次价格，以及对应的结算记录。`

`这让 AI 工作流第一次同时拥有了执行日志、成本明细和可核验的支付轨迹。`

English:

`This is the core of the product. What you see is not just a progress bar. It is a live billing stream. Every row represents a real security tool call, including the tool type, the target, the unit price, and the matching settlement record.`

`For the first time, the AI workflow has execution logs, cost breakdowns, and verifiable payment traces in one place.`

字幕 / Subtitle：

中文：

`每一行 = 一次调用、一笔成本、一张收据`

English:

`Every row = one tool call, one cost, one receipt`

### 5. 展示风险发现 / Show the Findings（0:48 - 1:03）

画面 / Visual：

中文：下拉到 Findings 区，展示 1-2 个高危或高优先级问题。

English: Scroll to the Findings section and show one or two high-severity or high-priority issues.

旁白 / Voiceover：

中文：

`系统并不是只会收费。随着流程执行，它会持续产出真实的安全结果，比如硬编码密钥、危险的 eval 调用、站点安全头缺失等问题，并把证据和建议绑定到对应的调用结果上。`

English:

`The system does more than bill. As the workflow runs, it continuously produces real security findings, such as hardcoded secrets, dangerous eval usage, and missing security headers, then binds the evidence and recommendations to the exact invocation that produced them.`

字幕 / Subtitle：

中文：

`系统产出的是真实发现，不只是支付事件`

English:

`The system produces real findings, not just payment events`

### 6. 讲清楚 Arc / Circle 的价值 / Explain the Value of Arc / Circle（1:03 - 1:18）

画面 / Visual：

中文：切到盈利矩阵、成本对比卡片，或者链上 / 结算证明区域。

English: Cut to the profitability matrix, cost comparison cards, or the on-chain / settlement proof section.

旁白 / Voiceover：

中文：

`这类 AI 安全流程的特点是调用频率高、单次价值低、数量又非常多。传统 gas 模型会迅速吞掉利润空间。Arc 配合稳定币微支付，才让这种按次计费的 AI 工作流真正具备经济可行性。`

`Circle 提供了稳定价值单位，Arc 提供了适合高频结算的底层环境。`

English:

`This kind of AI security workflow is high-frequency, low-value per call, and massive in volume. A traditional gas model quickly destroys the margin. Arc plus stablecoin micropayments makes this pay-per-call AI workflow economically viable.`

`Circle provides the stable unit of value, and Arc provides the settlement environment built for high-frequency execution.`

字幕 / Subtitle：

中文：

`微支付让高频 AI 安全工作流具备经济可行性`

English:

`Micropayments make high-frequency AI security economically viable`

### 7. 结尾 / Closing（1:18 - 1:30）

画面 / Visual：

中文：回到项目首页，或者放 GitHub / Demo / 项目名。

English: Return to the project homepage, or show the GitHub repo, demo URL, or project name.

旁白 / Voiceover：

中文：

`LaunchShield Swarm 让 AI 安全审计第一次可以按工具调用来计费、按调用来追踪、按调用来验证。`

`这就是我们为 Agentic Economy on Arc 提交的作品。`

English:

`LaunchShield Swarm makes AI security audits billable per tool call, traceable per tool call, and verifiable per tool call for the first time.`

`This is our submission for Agentic Economy on Arc.`

字幕 / Subtitle：

中文：

`LaunchShield Swarm`

`可审计。可计费。按调用结算。`

English:

`LaunchShield Swarm`

`Auditable. Billable. Per tool call.`

## 精简版口播 / Short Voiceover Version

中文：

`LaunchShield Swarm 是一个 AI 安全审计演示系统。我们把一次审计拆成很多原子化的工具调用，比如仓库扫描、依赖检查、浏览器探针、LLM 深度分析和修复建议。每一次调用都可以单独展示结果，也可以单独计费和结算。这样，执行流、成本流和证据流就被统一到了一个界面里。Arc 和 Circle 的价值在这里非常关键，因为高频、低单价的 AI 工作流只有在微支付模型下才具备可持续的商业可行性。`

English:

`LaunchShield Swarm is an AI security audit demo system. We break one audit into many atomic tool calls, including repo scans, dependency checks, browser probes, deep LLM analysis, and fix suggestions. Every call can show its own result, and every call can be billed and settled independently. That brings the execution flow, cost flow, and evidence flow into one interface. Arc and Circle are critical here because a high-frequency, low-ticket AI workflow only becomes sustainably viable under a micropayment model.`

## X 发帖文案建议 / Suggested X Post Copy

中文：

`LaunchShield Swarm 把 AI 安全审计变成可付费、可核验的微任务。一次审计会变成很多有价格的工具调用，比如仓库扫描、浏览器探针、LLM 审查和修复建议，并且它们都会被追踪在同一条实时执行流和账单流里。Built for the agentic economy on Arc. @buildoncircle @arc @lablabai`

English:

`LaunchShield Swarm turns AI security audits into paid, verifiable micro-tasks. One audit becomes many priced tool calls: repo scans, browser probes, LLM reviews, and fix suggestions, all tracked as one live execution and billing flow. Built for the agentic economy on Arc. @buildoncircle @arc @lablabai`

## 录制重点 / Must-Capture Points

- 中文：一定要拍到“运行起来”的过程
- English: Make sure the video shows the product actively running.
- 中文：一定要拍到账单流水连续增长
- English: Make sure the billing waterfall keeps growing on screen.
- 中文：一定要拍到 1-2 个真实发现
- English: Make sure you show one or two real findings.
- 中文：一定要讲清楚“为什么一定要微支付”
- English: Make sure you explain why micropayments are essential.
